package com.revature.Banking_App;




public class App 
{
    public static void main( String[] args )
    {
        /*If the default files need to be recreated, comment out 'DashBoard dash = new DashBoard()' and 'dash.Login()'*/
    	/*Remove the comments from 'Reset reset = new Reset()' and 'reset.ResetBaseOutputFiles()'*/
    	/*Run program once, then set the comments back to what they were*/
    	
    	//Reset reset = new Reset();
    	//reset.ResetBaseOutputFiles();
    	
    	DashBoard dash = new DashBoard();
    	dash.Login();
    	
    }
}
