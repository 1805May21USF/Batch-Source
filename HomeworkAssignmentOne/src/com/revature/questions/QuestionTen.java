package com.revature.questions;

public class QuestionTen {

		public QuestionTen() {
			
		}
		/*
		 * I don't understand ternary operators.
		 * But the following line checks (with ?)
		 * that a is less than b. then says a and b are
		 * inputs.
		 */
		public int run(int a, int b) {
			return (a < b) ? a : b;
		}
}
